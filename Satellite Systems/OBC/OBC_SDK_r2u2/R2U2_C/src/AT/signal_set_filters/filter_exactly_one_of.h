#ifndef FILTER_EXACTLY_ONE_OF_H
#define FILTER_EXACTLY_ONE_OF_H

#include <stdint.h>
#include <stdlib.h>
#include <stdbool.h>

#include "R2U2Config.h"

bool filter_exactly_one_of(bool *set, uint8_t len);

#endif
Update sequence:

1. Following the documentation first update both bootloader using the debugger and the provided .HEX file.
   This will upgrade the bootloader to version 2.00.
2. Next update the application using the provided GUI and the provided .SCRM file. Again, follow the documentation.
   This will update the application to version 2.05, which is the latest release.